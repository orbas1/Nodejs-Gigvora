CREATE DATABASE IF NOT EXISTS gigvora_dev CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE gigvora_dev;

-- The Sequelize migrations will create the tables. Run `npm run migrate` once scripts are added.
